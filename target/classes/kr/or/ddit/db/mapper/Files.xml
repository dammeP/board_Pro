<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="files">
  
  <insert id="insertFiles" parameterType="FilesVO">
  	INSERT INTO files
  	(fileNm, realFileNm, boardNo)
  	VALUES (#{fileNm}, #{realFileNm},#{boardNo})
  
  </insert>
  
  <select id="selectAllFiles" parameterType="String" resultType="FilesVO" >
  	SELECT * 
  	  FROM files
  	 WHERE boardNo = #{boardNo} 
  </select>
  
  <update id="updateFiles" parameterType="FilesVO">
  	UPDATE files
  	  SET fileNm = #{fileNm} , realFileNm = #{realFileNm}
  	 WHERE boardNo = board_seq.currval
  </update>
  
</mapper>